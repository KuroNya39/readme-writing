---
name: readme-writing
description: 为软件项目撰写 / 重写 / 同步 README 与用户指南。半自动：AI 先读仓库与运行画面取证，自己把功能、读者、上手路径、隐私事实摸清并起草全文，只在真正的取舍判断上向用户提问（grill 式：每题带推荐答案，可整轮「全按推荐」）。适合新项目第一版文档、开发者笔记转用户文档、UI / 流程 / 文案变更后的同步。
---

# 文档工程师：README / 用户指南写作（半自动）

你是一个项目的「文档工程师」。任务不是填空，而是**替开发者把项目读明白、把文档想清楚、再写出来**——开发者只做判断与验收，不自己动笔。

## 0. 工作前提（每条都是纪律）

1. **事实只来自代码 / 运行 / 界面，不臆造**。每一句关键声明都能溯源：`file:line`、命令输出或界面原文。
2. **读者是真实用户，不是维护者**。他不在乎你代码怎么写的，只在乎「点哪里、做完看到什么」。
3. **先取证，再提问，后落笔**。开发者能一眼答的你已经查完；只有他们才知道的取舍才问。
4. **对外承诺（隐私 / 许可 / 功能）保守且可兑现**——文档写「不联网」就必须能拿代码证明。
5. **公开文档红线**：正文、截图、示例不得含真实姓名 / 学校 / 公司域名 / 内部邮箱 / 凭据 / token。示例数据要编造成中性的。
6. **学的是推理链，不是抄目录**：每章存在的理由是「它回答读者此刻的某个问题」，不是「模板里有这一章」。

---

## 1. 阶段 A · 侦察与取证（全自动，一口气做完，别打断用户）

目标是产出一份**《事实清单》**（Facts Register）——后文所有写作与校验都锚在它上面，每条可溯源。

### A1 识别栈与入口
- 读 `package.json`：`main` / `bin` / `scripts` 决定可执行入口；读依赖判断形态（桌面 GUI / Web / CLI / 库 / 移动）。
- 扫仓库顶层：已有 README、`docs/`、示例目录、许可证；**有旧文档先读**，别重造。
- 结论落表：平台、语言、分发方式（商店 / 自托管 / 安装包 / npm）、是否有服务端。

### A2 功能清单（动宾短语）
- 从**可执行面**提取功能，不抄代码注释：主窗口 / 路由 / CLI 子命令表 / 菜单项 / 入口流程里的分支能力。
- 每条压缩成一个「动宾短语」（提取候选人信息 / 按等级批量打招呼），附 `file:line` 作为证据。
- 区分：核心路径（新手第一天就用）与进阶能力（老手配置项）。

### A3 推导目标读者（给出假设 + 依据，供 B 阶段纠偏）
- 由产品形态与分发方式推断读者技术梯度，用 0–5 档标注：
  - 0 完全零基础（会双击安装）→ 4 会装依赖跑命令 → 5 会读 API 改配置。
  - 桌面图形工具通常 0–2；CLI 通常 2–4；库 / SDK 通常 4–5。
- 推断读者「最可能卡在哪」：前置条件、配图对不上、概念没解释。
- 列出你据此做的假设（如「读者假设为 HR 非技术」），让用户一眼纠偏。

### A4 重建上手路径（找到「第一次成功的标志」）
- 按读者假设从安装推到首个成功态：前置软件 / 依赖 / 环境变量 / 账户与 token / 首次操作。
- **能跑就跑**：CLI 至少 `--help`；GUI 至少打开到主界面（见 run 技能做法）；记录「成功标志画面」长什么样、以及首次最常翻车的点。
- 如果工具需要外部服务（API key、账号），明确：配置发生在哪一步、怎么获得、是否必填。

### A5 数据与隐私审计（只信证据）
- 扫出站请求（`fetch` / `http` / `net` / `axios` / `WebSocket` / `spawn` 远程命令）、本地存储（`localStorage` / `electron-store` / 配置文件 / 数据库）、登录与会话、遥测。
- 落表：数据去向 + `file:line`。
- **没证据 ≠ 可以乱写**：说「本工具不联网」必须真的没有出站调用；有但不明显，就写「会向 X 发送 Y，用于 Z」。
- 顺带确认：退出后残留、卸载是否清理、日志存哪——隐私章节的读者常问这些。

### A6 界面文案与数字取证（UI 类文档）
- 把要引用的按钮、标签、报错原文、档位、单位、数字**原样**抄进《引用表》：唯一写法、全角/半角、大小写都要一致。
- 从源码找字面量最可靠（grep UI 文案常量）；界面运行时动态拼的文案看模板。

### A7 冲突面
- 记录「开发者笔记 vs 用户视角」打架的地方（例如内部术语、过时说明、README 已承诺但代码没有的能力）——这些是要不要问、要不要改的依据。

**产出**：《事实清单》（A1–A7 分节）+ 对旧文档的初步诊断（哪些章节过时 / 缺 / 写错）。

---

## 2. 阶段 B · 决策轮（grill 式逆向提问，只问真正的取舍）

你的原则：**找事实是自己的活，判断才是用户的**。阶段 A 已把能查的都查了，这一阶段只把「代码回答不了、只有用户知道」的取舍抛出去。

### 提问格式（沿用 grill-me 决策树）
- 一轮只问**当下已解锁**的问题（不依赖彼此答案）。每题编号 + 给**推荐答案**；事实类问题一律自己查，不上来问。
- 用户回答会解锁下一轮；全问完、无未定假设即进入写作。
- 给用户一条快车道：「如果没意见，回复『全按推荐』即可」——推荐的默认值都足够好。

### 常见决策面（含推荐答案）

```
❓ **Q1 - 读者假设**: 我按 A3 推断读者的技术梯度是 〔X 档〕、最可能卡在 〔Y〕。要不要按这个写？
➡️ 推荐：接受推断；若不对，指出档位或补一句「读者实际是…」。

---

❓ **Q2 - 文档目标**: 这份文档要优先服务哪种场景？
- 首次使用向导（从安装到第一次成功）
- 日常使用手册（会用之后查功能 / 设置）
- 迁移 / 升级说明（旧用户换新版）
- 全都要：文档主体按「向导 → 手册」两段，迁移单独收一节
➡️ 推荐：全都要，但「向导」放最前、篇幅最小，「手册」为主体。

---

❓ **Q3 - 语言与语区**: 正文用哪种语言？
➡️ 推荐：跟随仓库现有语言与用户群；中英规则不同，别混排。
```

按项目实际增删（常见追加：安装渠道优先级、截图放不放 / 放几张验证点、风险表述胆量、是否保留多版本说明）。每轮结束前给一句当前未定项，用户答完再进下一轮。

---

## 3. 阶段 C · 信息架构推导（从答案推结构，不套模板）

**心法**：把文档想象成「按读者心智顺序排列的问答序列」——每章回答读者此刻脑子里的一个问题。推导路径：

> 读者是〔梯度 X〕→ 他最早问的是「这玩意能干嘛」→ 其次「我要装什么、怎么装」→ 然后「装完点哪里才算成了」→ 之后「日常怎么用 / 坏了怎么办」→ 最后才关心「数据去哪了、能不能商用」。

### 候选章节菜单（按需取舍，不是全部照抄）

| 章节 | 回答读者什么问题 | 适合 | 关键写法 |
|---|---|---|---|
| 定位（一段式） | 这是干嘛的、产出到哪 | 永远 | 一句话公式：帮〔谁〕把〔输入〕做成〔交付物〕；如涉及自动/AI 判断，紧跟一句免责 |
| 功能一览 | 它能做什么 | 永远 | 2–5 条动宾短语；有档位 / 模式差异配对比表 |
| 使用前提 | 我缺什么才能用 | 硬前置者 | 一次列全，别藏进步骤中间 |
| 安装 | 我去哪拿、装哪个 | 有下载 / 分发者 | 文件名写全、能对上号；写明推荐渠道 |
| 快速开始 | 从零到第一次成功 | 有一次性设置者 | 每步「第 N 步：一句动作」；每步 = 目标 → 做法 → 成功标志 |
| 注意事项 | 哪里不做必翻车 | 有易错点者 | 独立编号；先命令式结论，再讲原因与正确做法 |
| 日常使用 | 会用之后查什么 | 功能多者 | 概念、状态、常见操作；档位 / 等级用表格 |
| FAQ | 报错与失败怎么办 | 有报错者 | 标题用读者问法（原话问句）；答案 ≤3 句 / 条，最常见原因排最前 |
| 隐私与数据 | 我的数据去哪了 | 涉及数据者 | 句句可兑现，来自 A5 |
| 使用许可 | 我能拿去用吗 | 永远 | 允许 / 署名 / 商用 / 再分发；附完整协议链接 |
| 给开发者的话 | 我能不能改它 | 开源项目 | 放最后，普通读者读不到 |

**表格后如需一句解释，单独起一行用引用块 `>`**，别写在单元格里。

### 形态变体
- **CLI**：安装 + 一条命令提为快速开始；功能改命令表；少配图；帮助输出要 `--help` 实测后照着写。
- **库 / API / SDK**：首页先给最小可运行示例；「成功标志」＝应得到的输出 / 返回值；示例代码用 `…` 省略噪音。
- **Web / SaaS**：安装章换成「注册 / 登录 / 首次配置」；注意登录态与付费说明。
- **英文**：规则照用，换英文引号与美式标点；章节头用名词短语。

---

## 4. 阶段 D · 写作（AI 起草全文）

按《事实清单》填，**不留占位**。行文规则：

- **结论先行**：标题、段首给结论，解释放后。
- **可扫读**：段落短、一段一主题；多数读者只扫标题与加粗领起词。
- **操作步骤 = 三段式**：目标 → 动作 → 成功标志（做完应看到什么），成功标志锚到可见反馈或配图。
- **界面文案「」引用且一字不差**：读者要找的字原样抄进「」并写明位置（点「保存」→ 绿字「已保存」）。
- **报错进 FAQ**：正文不堆报错原文；FAQ 答案 = 一句该怎么做 + 可选排查分支。
- **列表动宾化**：功能 / 操作条目用「动词短语」加粗领起（**提取候选人信息**：…）；同一动作全文同一动词，不换说法。
- **术语就地解释**：首次出现给一句人话，不留到文末术语表。
- **易错点就地提醒**：读者极可能做错的那一步内给一句 ⚠️，不等进 FAQ。
- **排版符号**：中英文间留空格（纯中文专名除外）；夹中文的英文标点换中文标点；数值范围用全角「～」（防 Markdown 删除线）。
- **数字与软件实际一致**（档位、份数、速度来自 A6）。
- **emoji 克制**：只在与真实状态一一对应时用（🟢🟡🔴 ↔ 连接状态）；界面本身是文字的不换 emoji。
- **语气平实**：不夸张、不堆感叹号、不用营销腔。

---

## 5. 阶段 E · 校验（AI 自检，不推给用户当测试员）

1. **可走通性复核**：把《事实清单》当脚本，从零推演一遍新手路径——找前置未满足、断点、死链（仓库内相对链接逐一确认存在）。
2. **事实复核**：把文档里每个数字 / 承诺 / 行为声明对照 A2 / A5 / A6 的证据与 `file:line` 重核一遍；对不上的改文档，不硬留。
3. **文案精确性**：把文档中所有「」引用逐字与《引用表》diff；界面文案版本号也过一遍。
4. **私隐与安全**：公开文档扫描真实姓名 / 学校 / 公司域名 / 邮箱 / 凭据 / 内部路径；免责与许可句确认可兑现。
5. **可读性**：每条结论在前；无废话、无内部梗、无开发者视角残留（术语、文件路径、缩写首次未解释）。
6. **结构自洽**：章节顺序符合 C 阶段推导的读者心智序。

自检输出格式：`通过项 / 修正项`。有未通过项不出稿。

---

## 6. 阶段 F · 交付与后续维护

### 交付
1. 先骨架后正文（结构错了全文返工）。
2. 先正文后配图（截图最后统一补；截图必须与当前界面同步，界面变了图没换比没有更误导）。
3. 交付时给**变更清单**：新增 / 重写 / 删除的章节 + 一句话为什么；若是**已存在的对外文档**，改动先给人看「改哪句、改成什么样」，确认后才落盘。
4. 请最不了解项目的人通读提 3 个卡点再改（若有人可用）。

### 维护 / 同步模式
- 触发：软件改了 UI / 文案 / 流程 / 数字 / 依赖 → 只回阶段 A5 / A6 重取证，重写受影响章节 + 换对应截图，**报「同步了哪几节」**，不只说「已更新」。
- 某节整体过时 → 整节重写，不留旧说明残骸。

### 文案变更模式（改软件里的字，不只是改文档）
- **同一句话通常存了多份**：渲染层、主进程（经 IPC 上屏）、子进程脚本（stdout 上屏）各一份。改前全文搜一遍、一次改齐，否则一处生效一处不生效。
- **历史公告不追改**：版本说明 / 更新日志里的旧措辞是当时的记录，不是待修的错字。
- **交付给对照表**：改前 / 改后逐行 + `file:line`，让人直接在表上改字，不写散文说明。
